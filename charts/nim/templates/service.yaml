apiVersion: v1
kind: Service
metadata:
  name: {{ include "nim.serviceName" . }}
  labels:
    {{- include "nim.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nim.selectorLabels" . | nindent 4 }}